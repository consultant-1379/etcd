enabledAuditors:
  # Auditors are enabled by default if they are not explicitly set to "false"
  # supress seccomp errors in kubeaudit report
  seccomp: false
  # supress apparmor errors in kubeaudit report
  apparmor: false